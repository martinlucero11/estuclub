rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ⚠️ REGLA DE EMERGENCIA
    // Todo permitido para lectura y escritura
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
