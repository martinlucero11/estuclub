import SplashScreen from '@/components/layout/splash-screen';

export default function Loading() {
  // This component is automatically shown when the app loads,
  // acting as our main splash screen.
  return <SplashScreen />;
}
